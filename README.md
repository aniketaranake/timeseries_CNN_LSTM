# timeseries_CNN_LSTM

See example.ipynb

This example uses `pipenv` to manage dependencies. To set up locally, use

```
$ pip3 install pipenv
$ pipenv --three
$ pipenv install --dev
$ pipenv run jupyter notebok
```
